# Per-user / per-machine config — copied from .env.example
.env

# OS / editor auto-backups that often shadow real files
*복사본*
* - Copy*
*.bak
*~

# Porter output — regenerated by `node port-omc.cjs`
output/

# Test scratch dirs created during PoC verification
test-tmp-*/

# apply.cjs creates timestamped backups when it overwrites existing config
*.backup-*

# Node / editor / OS
node_modules/
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
